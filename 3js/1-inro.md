# Web development :-
Web development involves writing a lot of HTML, CSS and JS code.
# JS :-
Any website that you see, is a bunch of HTML, CSS and JS files along with some assets(images, video etc).
# fact/Callouts :-
1. React, NextJS are framework. They compile down to HTML, CSS , JS in the end. That is what your browser unserstands.

# Properties of JS :-
1. interpreted.
2. single threaded
3. Garbage Collected : JavaScript automatically manages memory allocation and deallocation through garbage collection, which helps pervents memory leaks by automatically reclaiming memory used by objects no longer in use.
- memory managemt :-
    - ther are three way of memory management.
    1. Garbage collector
    2. Manual
    3. The rust way
     